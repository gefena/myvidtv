# /opsx-apply

Run the `openspec-apply-change` workflow for this workspace.

## Arguments

- `request`: change name to implement, or a clear instruction pointing at an existing OpenSpec change

## Workflow

1. Use the `openspec-apply-change` skill.
2. Implement the requested change against the existing OpenSpec artifacts.
3. Verify the work as appropriate for the touched files before summarizing.
