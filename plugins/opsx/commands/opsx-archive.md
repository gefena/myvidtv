# /opsx-archive

Run the `openspec-archive-change` workflow for this workspace.

## Arguments

- `request`: completed change name to archive

## Workflow

1. Use the `openspec-archive-change` skill.
2. Confirm the change is complete and archive it according to the workspace workflow.
3. Summarize the archive result and any follow-up state.
