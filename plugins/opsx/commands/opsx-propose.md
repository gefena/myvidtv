# /opsx-propose

Run the `openspec-propose` workflow for this workspace.

## Arguments

- `request`: desired feature, fix, or change name

## Workflow

1. Use the `openspec-propose` skill.
2. Create or continue the appropriate OpenSpec change under `openspec/changes/`.
3. Produce the artifacts needed to make the change ready for implementation.
