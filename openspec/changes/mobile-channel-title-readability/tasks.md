## 1. Update Mobile Channel Browse Rows

- [x] 1.1 Adjust the mobile title styling in `src/components/ChannelBrowseModal.tsx` so long channel video titles render across up to two readable lines before truncating
- [x] 1.2 Preserve the published date as a separate metadata line beneath the title while keeping each row a single tap target

## 2. Verify Mobile Browsing Behavior

- [x] 2.1 Verify the channel browse modal still scrolls cleanly and remains easy to scan on a small-screen viewport after the taller rows
- [x] 2.2 Run lint and build after the mobile channel title readability change
