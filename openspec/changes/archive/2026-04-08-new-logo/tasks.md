## 1. Favicon

- [x] 1.1 Replace `src/app/icon.svg` with the new viewport mark: dark rounded-rect background + inner frame stroke + violet circle at lower-left

## 2. Header mark + wordmark

- [x] 2.1 Replace the `▶` emoji in `Header.tsx` with an inline SVG viewport mark (24×24, rounded rect frame + violet circle)
- [x] 2.2 Replace the plain "MyVidTV" span with two-tone wordmark: `<span>"MyVid"</span><span style violet semibold>"TV"</span>`
